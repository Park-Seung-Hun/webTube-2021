extends ../layouts/main
include ../mixins/videoBlock

block content
    .videos 
        each video in videos   
            +videoBlock({
                title: video.title,
                views:video.views,
                videoFile:video.videoFile
            })    
        //-MongoDb에 들어가기전 작업
